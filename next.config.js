/** @type {import('next').NextConfig} */
const nextConfig = {
    // Vercel handles the build process automatically.
    // If you ever need Docker, add: output: 'standalone'
}

module.exports = nextConfig
